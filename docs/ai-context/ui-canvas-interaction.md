# Contexto IA — Canvas

El canvas debe ofrecer pan, zoom, selección, Ajustar y Restablecer. Los atajos
son `A`, `0`, `+` y `−`; deben aparecer en la ayuda contextual y conservar una
alternativa mediante botones visibles. No depender sólo del ratón.
