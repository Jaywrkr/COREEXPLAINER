# Cross-vendor delivery authority rules context

`src/content/cross-vendor-delivery-rules.ts` is the contract for implementation-lifecycle. Preserve vendor boundaries within one delivery path. Project-pattern evidence describes a process pattern only; it must never be used to assert supportability, compatibility or a customer's completed acceptance.
