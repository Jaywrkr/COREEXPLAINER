# Synology technical authority rules context

`src/content/synology-technical-rules.ts` maps Synology source IDs to narrated scenes. Preserve the distinction between a file service, directory-based access, HA and backup; never use an HA source as evidence that a file is recoverable.
