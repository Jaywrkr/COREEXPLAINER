# Check Point technical authority rules context

`src/content/checkpoint-technical-rules.ts` is the source-to-scene contract for Check Point HA. Claims about ClusterXL, compatibility, state synchronization and failover must keep their separate official source IDs and cited step IDs.
